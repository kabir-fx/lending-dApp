/**
 * Re-export the generated client for the lending protocol.
 * This exposes program addresses, accounts, and instruction builders
 * directly under the `@project/anchor` path used by the frontend.
 */
export * from './client/js'
